require 'rails_helper'

RSpec.describe Destination, type: :model do
  before do
    # @india = Destination.new(name: "India")
    # @ukraine = Destination.new(name: "Ukraine")
  end
  
  it "sorts them in alphabetical order" do 
    # Destination.all.sort 
  end

  describe "Country select box" do 
    it "replaces markers with only those for that country and refocuses map" do

    end


  end
end
